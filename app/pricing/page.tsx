import Link from 'next/link'
import { Check } from 'lucide-react'
import { PageFrame, PageHero, SectionLabel } from '@/components/site-shell'

const plans = [{ name: 'Solo', price: 'Free', note: 'For building a steady habit.', features: ['All five practice paths', '15+ guided lessons', 'Local progress history', 'Character analytics'] }, { name: 'Studio', price: '$12', note: 'For people who want a deeper routine.', features: ['Everything in Solo', 'Custom lesson collections', 'Weekly momentum review', 'Exportable progress notes'] }, { name: 'Team', price: '$8', note: 'Per person, for shared practice.', features: ['Everything in Studio', 'Team lesson tracks', 'Cohort progress view', 'Shared practice prompts'] }]
export default function PricingPage() { return <PageFrame><PageHero eyebrow="Pricing / Clear and calm" title={<>A plan for your <span className="text-primary">next habit.</span></>} description="Start with the complete core practice experience. Upgrade when you need a more structured way to keep a group or personal routine moving." action="Try Solo free" /><section className="mx-auto grid max-w-7xl gap-4 px-5 pb-20 md:grid-cols-3 md:px-8">{plans.map((plan, index) => <article key={plan.name} className={`card p-7 ${index === 1 ? 'border-primary bg-gradient-to-b from-primary/15 via-card to-primary/5 shadow-[0_28px_70px_-28px_color-mix(in_oklch,var(--primary)_45%,transparent)]' : ''}`}><div aria-hidden className="card-glow" /><div className="relative flex items-start justify-between"><div><SectionLabel>{index === 1 ? 'Most focused' : 'For you'}</SectionLabel><h2 className="text-2xl font-medium">{plan.name}</h2></div><span className="font-mono text-2xl">{plan.price}<small className="ml-1 text-xs text-muted-foreground">{plan.price !== 'Free' && '/ mo'}</small></span></div><p className="relative mt-5 text-sm leading-6 text-muted-foreground">{plan.note}</p><ul className="relative mt-8 flex flex-col gap-3 border-t border-border pt-6">{plan.features.map((feature) => <li key={feature} className="flex gap-2 text-sm"><Check className="text-primary" />{feature}</li>)}</ul><Link href="/practice" className="relative mt-9 block rounded-full border border-border px-4 py-3 text-center text-sm font-medium transition-colors hover:border-primary hover:bg-primary hover:text-primary-foreground">Choose {plan.name}</Link></article>)}</section><section className="border-t border-border bg-muted/40"><div className="mx-auto max-w-4xl px-5 py-16 md:px-8"><SectionLabel>Good to know</SectionLabel><div className="grid gap-6 md:grid-cols-2"><div><h3 className="font-medium">No pressure to perform</h3><p className="mt-2 text-sm leading-6 text-muted-foreground">The free experience is not a sample. It is the full loop: choose, practice, measure, return.</p></div><div><h3 className="font-medium">Progress is yours</h3><p className="mt-2 text-sm leading-6 text-muted-foreground">Your local momentum and character signals never depend on a hidden paywall.</p></div></div></div></section></PageFrame> }
